# DESACTIVE (synergie 23/07). Tout est consolide dans station.yml.
# Ne lance plus rien -> zero doublon, zero risque de double ordre reel.
# Le controle passe par station.yml + les commandes Telegram (stopreel, etc.).
name: reel-mainnet (desactive)
on:
  workflow_dispatch: {}
permissions:
  contents: read
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Workflow desactive. Toute la station tourne dans station.yml (consolidation synergie 23/07)."
